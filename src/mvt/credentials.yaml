cookie:
  expiry_days: 30
  # Must be a string
  key: some_signature_key
  name: some_cookie_name
credentials:
  usernames:
    guest:
      email:
      # Will be managed automatically
      failed_login_attempts: 0
      first_name: Guest
      last_name: User
      # Will be managed automatically
      logged_in: False
      # Will be hashed automatically
      password: guest
      # Optional
      roles: guest
    gcapuzzi:
      email: gianluca.posta78@gmail.com
      # Will be managed automatically
      failed_login_attempts: 0
      first_name: Gianluca
      last_name: Capuzzi
      # Will be managed automatically
      logged_in: False
      # Will be hashed automatically
      password: gpassword
      # Optional
      roles: user
    bobbi:
      email: bobbi@ledgeracademy.com
      # Will be managed automatically
      failed_login_attempts: 0
      first_name: Bobbi
      last_name: Muscara
      # Will be managed automatically
      logged_in: False
      # Will be hashed automatically
      password: dpassword
      roles: admin
